{
  "name": "@devgertschi/fml-sdk",
  "version": "0.2.0",
  "description": "Freeform Markup Language (FML) SDK for structured AI prompting using XML-style tags and variables.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "link" : "npm run build && npm link",
    "superclean": "npm run clean && rm -rf node_modules && rm -rf package-lock.json && npm cache clean --force",
    "test": "vitest"
  },
  "keywords": [
    "fml",
    "prompt",
    "prompting",
    "ai",
    "llm",
    "markup",
    "sdk",
    "openai",
    "anthropic",
    "xml"
  ],
  "author": "devgertschi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/devgertschi/fml-sdk"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/mustache": "^4.2.6",
    "@types/node": "^20.17.57",
    "@types/xml2js": "^0.4.14",
    "globals": "^16.3.0",
    "typescript": "^5.8.3",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "mustache": "^4.2.0",
    "xml2js": "^0.6.2"
  }
}
